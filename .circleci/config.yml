# Java Gradle CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-java/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/python:2.7.13

      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/postgres:9.4

    working_directory: ~/repo

    environment:
      # Customize the JVM maximum heap limit
      JVM_OPTS: -Xmx3200m
      TERM: dumb

    steps:
      - checkout

#      # Download and cache dependencies
#      - restore_cache:
#          keys:
#          - v1-dependencies-{{ checksum "requirements.txt" }}
#          # fallback to using the latest cache if no exact match is found
#          - v1-dependencies-
#
#      - run: pip install -r requirements.txt
#
#      - save_cache:
#          paths:
#            - "~/.cache/pip"
#          key: v1-dependencies-{{ checksum "requirements.txt" }}

      # add key from SSH Permissions
      - add_ssh_keys:
          fingerprints:
            - "cc:bc:63:58:1f:de:7c:d1:45:f6:c7:5b:9a:f2:37:82"

      - run: git config --global user.email "arndt@menedev.de" > /dev/null 2>&1
      - run: git config --global user.name "Michael Arndt" > /dev/null 2>&1
      - run: git clone git@github.com:MeneDev/ansible-role-docker-compose.git ~/menedev.docker-compose

      # generate
      - run: python read.py

      - run: cd ~/menedev.docker-compose && git add vars/main.yml
      - run: cd ~/menedev.docker-compose && git diff-index --quiet HEAD || git commit -m "updated vars/main.yml"

      # specify other key (name corresponds to fingerprint)
      - run: cd ~/menedev.docker-compose && GIT_SSH_COMMAND='ssh -i ~/.ssh/id_rsa_ccbc63581fde7cd145f6c75b9af23782' git push origin master
